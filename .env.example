# Threat Intelligence Platform Configuration
# Copy this file to .env and fill in your actual values

# Mandiant Credentials
MANDIANT_EMAIL=your_email@example.com
MANDIANT_PASSWORD=your_password
MANDIANT_TOTP_SECRET=your_base32_totp_secret_from_google_authenticator

# CrowdStrike Credentials
CROWDSTRIKE_EMAIL=your_email@example.com
CROWDSTRIKE_PASSWORD=your_password
CROWDSTRIKE_TOTP_SECRET=your_base32_totp_secret_from_google_authenticator

# Enrichment API Keys (Optional)
VIRUSTOTAL_API_KEY=your_virustotal_api_key
IPINFO_API_KEY=your_ipinfo_api_key
URLVOID_API_KEY=your_urlvoid_api_key

# How to get your TOTP Secret:
# 1. When setting up 2FA in Mandiant/CrowdStrike, you'll see a QR code
# 2. Look for "Can't scan?" or "Manual entry" option
# 3. Copy the base32 secret key (usually 16 characters, like: JBSWY3DPEHPK3PXP)
# 4. Paste it as the TOTP_SECRET value above
# 5. This allows automatic 2FA without manual token entry

# Security Note:
# - Keep this file secure and never commit it to version control
# - The .env file is already in .gitignore for protection
# - TOTP secrets are equivalent to physical 2FA devices - protect them accordingly